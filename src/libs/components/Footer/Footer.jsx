import React from "react";
import PropTypes from "prop-types";

function Footer() {
  return (
    <React.Fragment>
      <section></section>
    </React.Fragment>
  );
}

Footer.propTypes = {
  menu: PropTypes.string,
};

export default Footer;
