import React, {  } from "react";
import PropTypes from "prop-types";

function Header() {
  return (
    <React.Fragment>
      <section></section>
    </React.Fragment>
  );
}

Header.propTypes = {
  menus: PropTypes.string,
};

export default Header;
